services:
  - type: web
    runtime: python
    name: python-vision-recognizer
    region: oregon
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      export PYTHONPATH=src
      python -m python_vision.server
    envVars:
      - key: PORT
        value: "5001"
